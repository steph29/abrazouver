/// Sur mobile : affiche un message. Pour un vrai téléchargement, ajouter path_provider + open_file.
Future<bool> downloadFile(List<int> bytes, String filename) async {
  // TODO: implémenter avec path_provider + open_file pour mobile
  return false;
}
